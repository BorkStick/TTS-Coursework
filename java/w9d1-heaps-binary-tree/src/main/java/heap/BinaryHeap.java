package heap;


import java.util.ArrayList;
import java.util.List;

public class BinaryHeap {
    List<Integer> heap;

    public BinaryHeap() {
        heap = new ArrayList<>();
        heap.add(null);
    }

    public void minAdd(int value) {
        if (heap.size() > 1) {
            int par
        }
    }
}
